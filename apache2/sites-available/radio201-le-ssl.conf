<IfModule mod_ssl.c>
<VirtualHost *:443>
        # Descomentar Luego
	ServerName radio201.eie.ucr.ac.cr
        # Poner un correo válido ej: soporte.eie@ucr.ac.cr
	ServerAdmin webmaster@localhost

        DocumentRoot /var/www/radio201/
        <Directory />
                Options FollowSymLinks
                AllowOverride None
        </Directory>
        <Directory /var/www/radio201>
                Options Indexes FollowSymLinks MultiViews
		Require all granted
	</Directory>

        #ScriptAlias /cgi-bin/ /usr/lib/cgi-bin/
        #<Directory "/usr/lib/cgi-bin">
        #        AllowOverride None
        #        Options +ExecCGI -MultiViews +SymLinksIfOwnerMatch
	#	Require all granted
        #</Directory>

        ErrorLog ${APACHE_LOG_DIR}/error.log

        # Possible values include: debug, info, notice, warn, error, crit,
        # alert, emerg.
        LogLevel warn

        CustomLog ${APACHE_LOG_DIR}/access.log combined



SSLCertificateFile /etc/letsencrypt/live/radio201.eie.ucr.ac.cr/fullchain.pem
SSLCertificateKeyFile /etc/letsencrypt/live/radio201.eie.ucr.ac.cr/privkey.pem
Include /etc/letsencrypt/options-ssl-apache.conf
</VirtualHost>
</IfModule>
